{
  "name": "agent-readability",
  "version": "0.1.0",
  "private": true,
  "description": "Generate and validate llms.txt for the Docs Systems Lab synthetic docs corpus.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test \"dist/test/*.test.js\"",
    "gen": "tsc && node dist/src/cli.js gen",
    "validate": "tsc && node dist/src/cli.js validate"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
